# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RoomDecoratorMyApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RoomDecoratorMyApp
  pod 'RxSwift',    '~> 4.0'
    pod 'RxCocoa',    '~> 4.0'
    pod 'Firebase/Core'
    pod 'Firebase/Firestore'
    pod 'PopupDialog'

  target 'RoomDecoratorMyAppTests' do
    inherit! :search_paths
    # Pods for testing 
  end

  target 'RoomDecoratorMyAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
